/**
 * Default root directory for FairGarden Docs build caches. Sits alongside the marker
 * directories already written under `.next/cache/fairgarden-docs`.
 */
export const DEFAULT_CACHE_DIR = '.next/cache/fairgarden-docs';
