import { createTypes } from '@/functions/createTypes';
import { abstractCreateDemo } from '@fairgarden/docs/abstractCreateDemo';

export const TypesAbstractCreateDemo = createTypes(import.meta.url, abstractCreateDemo);
